{% extends 'base.html.twig' %}

{% block body %}
    <h1>Market</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ market.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ market.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('market_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('market_edit', { 'id': market.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
